language: ruby
addons:
  apt:
    packages:
    - make
    - texlive-fonts-extra
    - texlive-fonts-recommended
    - texlive-generic-extra
    - texlive-latex-extra
    - texlive-pictures
    - texlive-science
    - texlive-bibtex-extra

script:
  - make
  - mkdir -p pdf
  - mv thesis.pdf pdf/

branches:
  only:
  - master

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: pdf
  on:
    branch: master
